<%= form_with(model: order_line_status, local: true) do |form| %>
  <% if order_line_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_line_status.errors.count, "error") %> prohibited this order_line_status from being saved:</h2>

      <ul>
        <% order_line_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Order_Line_Status_Name %>
    <%= form.text_field :Order_Line_Status_Name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
